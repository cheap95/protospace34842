<main class="main">
  <div class="inner">
</div>
      <div class="greeting"> 
        <% if user_signed_in?%>
        <%= link_to "こんにちは、" ,root_path, class: :greeting__link%>
        <%= link_to current_user.name, user_path(current_user.id), class: :greeting__link%>
        <% end %>
      </div>
    <div class="card__wrapper">
      <%= render partial: "prototype", collection: @prototypes %>
    </div>
  </div>
</main>
